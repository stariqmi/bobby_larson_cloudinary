{
  "name": "photo_cloudinary",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify lib/browser.js -o static/bundle.js",
    "watch": "watchify lib/browser.js -o static/bundle.js --debug --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserify": {
    "transform": [
      "brfs"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "brfs": "^1.3.0",
    "jquery-ui": "^1.10.5"
  }
}
